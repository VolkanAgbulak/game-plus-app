import React from 'react';

const StateFilterComponent = (props) => {
    const {} = props;

    return (
        <>
            StateFilterComponent
        </>
    );
};

export default StateFilterComponent;
